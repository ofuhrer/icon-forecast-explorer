/**
 * Animation controls — section marker module.
 *
 * The animation logic (``animationIntervalMs``, ``updateAnimationUi``,
 * ``restartAnimationTimer``, ``startAnimation``, ``stopAnimation``,
 * ``stepAnimation``) lives in ``main.js`` under the
 * ``// ─── Section: Animation ───`` block.  It is tightly coupled to
 * ``state``, ``els``, and the ``animationTimer`` module-level variable.
 *
 * This file exists as a placeholder so that the module reference in
 * ``static/index.html`` and ``AGENTS.md`` is accurate.  Nothing is exported
 * from here yet; future work can migrate the functions here using a factory
 * pattern.
 */
