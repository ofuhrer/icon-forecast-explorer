[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint.per-file-ignores]
# weather_data.py is intentionally excluded from this refactoring PR;
# unused imports will be addressed in a follow-up cleanup.
"weather_data.py" = ["F401"]
